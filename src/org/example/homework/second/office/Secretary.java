package org.example.homework.second.office;

public class Secretary {
    public void keepCalmAndWait(Boss boss, Manager manager, Security security) {
        System.out.println(boss.name + " не волнуйтесь, " + manager.name + " все успеет. " + security.name + " - подождите!");
    }
}
